# Copy this file to backend/.env and fill in real values.

# Required. Used by services/gemini_service.py to render sprint tickets.
# Get one from Google AI Studio.
GEMINI_API_KEY=

# Optional. Max concurrent Gemini API calls when rendering sprint tickets.
# Higher = faster wall-clock time but more burst load against the free-tier
# rate limit (RPM). Defaults to 5 if unset.
GEMINI_MAX_CONCURRENCY=5

# Optional. Per-prescription timeout (seconds) for a single Gemini call
# before it's treated as failed and falls back to the plain-text ticket.
# Defaults to 30 if unset.
GEMINI_REQUEST_TIMEOUT_SECONDS=30

# Optional. Caps Gemini ticket rendering to only the top-K ranked
# prescriptions (already sorted by roi_score before this stage runs); any
# remainder gets the plain-text fallback instead of an LLM call. Bounds
# worst-case latency independent of dataset size. 0 = no cap. Defaults to 0
# (disabled) if unset.
GEMINI_TOP_K=0

# Optional. Isolation Forest anomaly score cutoff (0.0-1.0). Rows scoring at or
# above this are flagged for pattern matching. Defaults to 0.65 if unset.
ANOMALY_THRESHOLD=0.65

# Optional. Rejects uploads larger than this many megabytes. Defaults to 50 if unset.
MAX_UPLOAD_SIZE_MB=50

# Optional. Comma-separated list of origins allowed to call this API (CORS).
# Defaults to "*" (any origin) if unset -- fine for local development, but set this
# to your actual frontend URL(s) before deploying.
ALLOWED_ORIGINS=*

# Required for auth (GET /auth/me) and Supabase-backed persistence.
# Project URL and the publishable ("anon") key -- from Supabase dashboard -> API settings.
SUPABASE_URL=
SUPABASE_KEY=

# Required for any backend write that must bypass Row Level Security (RLS) --
# e.g. writing reports/prescriptions on a user's behalf once /pipeline/run exists.
# Supabase dashboard -> Project Settings -> API -> service_role secret. Never expose
# this to a frontend/browser context; backend-only.
SUPABASE_SERVICE_ROLE_KEY=

# NOTE: JWT verification (backend/api/auth.py) does NOT use a SUPABASE_JWT_SECRET
# despite the TRD listing one -- this project's Supabase Auth signs tokens with
# ES256 against a rotating public key set at <SUPABASE_URL>/auth/v1/.well-known/jwks.json,
# not a static HS256 shared secret, so no such secret applies here.

# Required. The single-tenant fallback identity every request runs as now that
# the login/signup UI has been removed (backend/api/auth.py). Must be a real
# Supabase Auth user id (uploads/reports have a foreign key to auth.users), so
# create one via the Admin API rather than inventing a UUID -- see CLAUDE.md's
# "Single-tenant mode" section for the exact command. A real Authorization:
# Bearer <jwt> header still works and takes priority over this fallback, so
# nothing here blocks reintroducing real multi-user auth later.
DEFAULT_USER_ID=
